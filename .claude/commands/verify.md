---
description: Verifica il progetto — typecheck, test e build
---

Esegui la verifica completa di Primi Segni (Node 24):

1. `npm run typecheck` — TypeScript su tutti i workspace.
2. `npm run test` — unit test (Vitest), in particolare il rule engine.
3. `npm run build` — build di produzione della PWA.

Riporta l'esito di ciascuno step. Se qualcosa fallisce, mostra l'output rilevante e fermati senza "aggiustare" in silenzio.
